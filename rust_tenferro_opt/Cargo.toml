[package]
name = "poisson_tenferro_opt"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tenferro-runtime = "0.3"
tenferro-cpu = "0.3"
plotters = { version = "0.3", default-features = false, features = [
    "bitmap_backend",
    "bitmap_encoder",
    "surface_series",
    "ttf",
] }
